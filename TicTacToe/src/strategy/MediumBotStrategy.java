package strategy;

import model.Board;
import model.Move;

public class MediumBotStrategy implements BotPlayingStrategy{
    @Override
    public Move makeMove(Board board) {
        return null;
    }
}

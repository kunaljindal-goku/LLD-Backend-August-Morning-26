package strategy;

import model.Board;
import model.Move;

public class HardBotStrategy implements BotPlayingStrategy{
    @Override
    public Move makeMove(Board board) {
        return null;
    }
}

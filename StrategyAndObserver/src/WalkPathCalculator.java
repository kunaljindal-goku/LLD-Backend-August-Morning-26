public class WalkPathCalculator implements PathCalculator{
    @Override
    public Path findPath(String source, String dest) {
        // algo to find path via Walk
        System.out.println("Going via Walk");
        return new Path();
    }
}

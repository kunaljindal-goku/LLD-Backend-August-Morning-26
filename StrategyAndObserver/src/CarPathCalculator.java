public class CarPathCalculator implements PathCalculator{

    @Override
    public Path findPath(String source, String dest) {
        // algo to find path via Car
        System.out.println("Going via Car");
        return new Path();
    }
}
